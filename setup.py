# This file contains metadata necessary to package our code into a module
from setuptools import setup

setup(
   name='mssql-compare',
   version='1.0',
   description='',
   author='Joseph Woolf',
   packages=['src'],
   #install_requires=['flask', 'flask_restful', 'requests', 'jsonpickle'], #external packages as dependencies
)
